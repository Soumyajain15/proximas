
// Firebase has been removed from this application.
// This file is kept as a stub to prevent build errors from potential lingering imports.

export const app = undefined;
export const authInstance = undefined;

if (typeof window !== 'undefined') {
  console.warn("Firebase has been removed from this application. Firebase-related functionalities will not work.");
}
